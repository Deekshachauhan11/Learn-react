Start learning about the react. In react i learn about the components, props and css-modules and use tailwind in react.
feature based and atomic based folder structure
functions
7.Hooks - it's like a magical function - useState(it's manage state ), useRef(ye element ko select karne m help karta h), useEffect(ek sath do se jada event perform karne m help karta h), use Context, useReducer(help to manage complex problem).
useState
form handling
To way binding
Router:- basic setup, nested routes, dynamic routes, usenavigate and 404 page.
Context API

